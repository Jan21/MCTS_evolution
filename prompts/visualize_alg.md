I want to visualize the  MCTS exploration implemented in MCTS/v1.py. I want an html page which will enable me to step throught the progress of the algorithm. I want three panes in which I will see 1. the mcts tree, 2. what happens at a given node 3. how does the current DAG of the current partial plan looks like. Could you suggest how this could be done? Dont implement anything, just suggest what are the useful things to have in the visualization