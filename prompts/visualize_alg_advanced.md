I want to visualize the  MCTS exploration implemented in MCTS/v1.py. I want an html page which will enable me to step throught the progress of the algorithm. I want several panes in which I will see, the mcts tree, , what happens at a given node, how does the current DAG of the current partial plan looks like, etc. One pane will be special because it will show the execution of a simplified pseudo code. It will be like a custom debugger. It will show what happens when the code goest through different lines. I dont want to see all functions, just the most important ones which influence the MCTS search. Other things should be abstracted. Could you suggest how this could be done? You will need to make a solid plan first because it is a complicated project.