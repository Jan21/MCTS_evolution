# Move-based planner — moved

The move-based (primitive-move policy + optimal-cost value) reformulation now lives in
its own package: **[`move_planner/`](move_planner/)**.

See **[`move_planner/README.md`](move_planner/README.md)** for the full writeup: the MDP,
the network input encoding, architecture, the optimal-cost oracle, data pipeline,
training setup + results, evaluation, and the path to self-play.
